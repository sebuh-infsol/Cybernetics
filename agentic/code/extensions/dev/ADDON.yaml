apiVersion: ops.aiwg.io/v1
kind: OpsExtension
metadata:
  name: dev
  labels:
    domain: dev-operations
spec:
  extends: ops-complete
  description: "DevOps — CI/CD pipelines, build automation, fleet-wide tooling"
  version: "1.0.0"
  capabilities: auto-discover
