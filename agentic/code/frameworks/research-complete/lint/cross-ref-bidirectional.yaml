id: research/cross-ref-bidirectional
name: Bidirectional cross-references
description: Related/See Also references should exist in both directions between REF notes
severity: info
applies-to:
  glob: findings/REF-*.md
checks:
  - type: cross-ref-bidirectional
    referencePattern: "REF-\\d{3}"
