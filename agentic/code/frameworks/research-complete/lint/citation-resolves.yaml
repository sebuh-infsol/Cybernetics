id: research/citation-resolves
name: Citation reference integrity
description: Every REF-XXX reference in body text points to an existing note in findings/
severity: error
applies-to:
  glob: "**/*.md"
checks:
  - type: reference-resolves
    referencePattern: "REF-\\d{3}"
    basePath: findings
